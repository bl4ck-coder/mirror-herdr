# A15 Sessions-critical skills (Hop C minimum)

Drafted in-fork (not live Hermes skill install unless Nacho OK):

1. `session-link-kanban-herdr`
2. `desktop-sessions-tab`
3. `herdr-bridge-os`
4. `ports-health`

Backlog (A15 full pack, not this hop): hop-circuit-steward, handoff-packet-hermes, duplex-kanban-done, umbrella herdr-inside-desktop, twins, Phase C mounts.

**MiMo:** shadow/support / volume only — no authority over commits or judges.
**Judge path:** `/p/encuentro-k3` (or profile `judge`) after builder.
**Jev gate:** `/p/jev` thin yes/no — non-blocking UX.
**Sandhi cut path:** orchestrate/hard-neutral only; product hops = Grok builder + k3 judge; Nous OFF.
