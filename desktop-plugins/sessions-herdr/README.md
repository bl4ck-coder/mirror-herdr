# sessions-herdr (Hop B)

Hermes Desktop disk plugin. Install copy:

`%LOCALAPPDATA%\hermes\desktop-plugins\sessions-herdr\plugin.js`

Enable/reload: Desktop ⌘K → Reload desktop plugins (or wait for watch).

Not a second kanban board. Gateway owns status via :8642/health (+ optional host.request).
